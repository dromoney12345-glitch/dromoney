import React from 'react';
import { Navigate } from 'react-router-dom';

/** Old onboarding course CMS removed. Legal copy lives in Layout Manager. */
const DocumentsCMS = () => <Navigate to="/admin/layout" replace />;

export default DocumentsCMS;
